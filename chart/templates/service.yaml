apiVersion: v1
kind: Service
metadata:
  name: {{ include "notifo.fullname" . }}
  labels:
    {{- include "notifo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notifo.selectorLabels" . | nindent 4 }}
